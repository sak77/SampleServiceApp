package com.example.sampleserviceapp

import android.app.Application

/**
 * Application class extends ContextWrapper.
 * It is a non-UI context which is present during lifetime
 * of the application.
 */
class MyApplication : Application() {
}